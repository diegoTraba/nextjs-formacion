// import type { NextConfig } from 'next';

// const nextConfig: NextConfig = {
// };

// export default nextConfig;
module.exports = {
    eslint: {
      // Desactivar ESLint en producción (para despliegues)
      ignoreDuringBuilds: true,
    },
  }
